# {feature-name} — SPEC

## 목적

<!-- 이 기능이 무엇을 하는지 한두 문장으로 -->

## 범위

- 포함: <!-- 이번 기능이 다루는 것 -->
- 제외: <!-- 다루지 않는 것. 범위 밖을 명시해야 나중에 스코프가 새지 않는다 -->

## 관련 엔티티 (근거: `docs/domain/erd.md`)

<!-- 이 기능이 다루는 테이블·필드를 나열. 필드명은 ERD와 정확히 같은 표기를 쓴다 -->

## 규칙

<!-- "무엇을·언제·어떤 조건에서" 형태로 구체적으로 쓴다. 모호한 서술("적절히 처리한다" 등) 금지.
     번호를 매겨서 이후 api-contract.md·prototype/에서 "규칙 N" 형태로 참조할 수 있게 한다. -->

1. ...

## 크기 기준

이 문서가 300줄을 넘으면 분리를 검토하고, 500줄을 넘으면 분리한다. 단, 분리 기준은 길이가 아니라
**서로 안 읽어도 되는 독립된 책임**이 쌓였을 때다 (`sdd-framework/evolution-rules.md`). 같은 엔티티의
생애주기를 다루는 내용이면 길어도 한 파일로 유지한다.

## 비고

<!-- 이 기능이 실제 MVP 범위가 아니라 샘플/실험용이면 여기 명시한다 (features/sample-login/ 참고) -->
