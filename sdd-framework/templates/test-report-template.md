# {기능} 테스트 결과

담당자:            테스트 날짜:
테스트한 커밋:  (git rev-parse --short HEAD 결과를 붙여넣는다. 커밋이 아직 없으면 "커밋 전")

## 자동 검증

- [ ] `npx tsx prototype/run.tsx` 통과 (PASS 개수:    , FAIL 개수:    )

## spec.md 규칙별 확인

`spec.md`의 번호 매긴 업무 규칙마다 한 줄씩 적는다. 규칙이 5개면 5줄이다 — 몇 개를 실제로
확인했는지 개수 자체가 이 기능이 얼마나 검증됐는지를 보여준다. 확인 안 한 규칙은 숨기지 말고
"안 함"이라고 솔직하게 적는다 ("확인 안 한 규칙이 있다"는 사실 자체가 팀장에게 필요한 정보다).

| spec 규칙 번호 | 어떻게 확인했나 (run.tsx의 테스트 이름, 또는 직접 확인한 방법) | 결과 |
|---|---|---|
| 1 |  | 통과 / 실패 / 안 함 |

## ux-philosophy.md §6 자체 점검 (`design/`이 있는 기능만 — 백엔드 전용 기능은 "해당 없음")

`ux-philosophy/ux-philosophy.md` §6 검증 기준표 7개 항목마다 이 화면이 어떻게 충족하는지,
충족하지 못하면 왜인지 한 줄씩 적는다. `integration-workflow.md`의 팀장 통합 게이트에 있는
같은 검증을 대신하지 않는다 — 그 앞에 두는 담당자 자체 점검이다.

| 검증 항목 | 이 화면에서 어떻게 충족하는가 (또는 왜 못 하는가) |
|---|---|
| 상태 이해 |  |
| 근거 이해 |  |
| 작업 보호 |  |
| 복구 가능성 |  |
| 선택권 |  |
| 비파괴성 |  |
| 접근 가능성 |  |

## 아직 안 되는 것 (Known Issues)

- 없으면 "없음"

## 팀장에게 물어봐야 하는 것

- 없으면 "없음"
