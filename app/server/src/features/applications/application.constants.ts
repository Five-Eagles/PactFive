/**
 * 원본: features/applications/prototype/server/application.constants.ts
 * Mock 전용 상수(테스트 유저 ID 등)는 옮기지 않았다 — app/에는 실제 사용자가 온다.
 */
export const ACCEPT_IDEMPOTENCY_PREFIX = 'application-accept-';
