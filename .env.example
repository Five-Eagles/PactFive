# 준영 구간만. 값은 넣지 마세요. 로컬 `.env`에만 실제 키를 둡니다 (gitignore됨).
# Kakao/Supabase 키 이름은 오민혁·최윤석 범위다.

# 위젯·공개 가능. 프론트 번들에 넣으려면 별도 공개 경로를 탄다.
PG_CLIENT_KEY=

# 서버 전용. VITE_ 금지. 없으면 PaymentGateway Mock만 쓴다.
PG_SECRET_KEY=
