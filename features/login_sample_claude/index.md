# login_sample_claude

`sdd-framework/feature-workflow.md`의 절차(SPEC → API 계약/디자인 시안 → Mock+구현 초안)를
"Claude" 역할의 AI가 독립적으로 밟은 시뮬레이션 산출물. `features/login_sample_codex/`,
`features/login_sample_cursor/`와 같은 login 기능을 각각 독립적으로 구현해 비교한다.

MVP 8개 기능에 포함되지 않으며 `app/`에 통합되지 않는다.
