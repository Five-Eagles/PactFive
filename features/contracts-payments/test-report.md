# contracts-payments 테스트 결과

담당자:            테스트 날짜:
테스트한 커밋:  (git rev-parse --short HEAD 결과를 붙여넣는다. 커밋이 아직 없으면 "커밋 전")

## 자동 검증

- [ ] `npx tsx prototype/run.tsx` 통과 (PASS 개수:    , FAIL 개수:    )

## spec.md 규칙별 확인

`spec.md`의 번호 매긴 업무 규칙마다 한 줄씩 적는다. 규칙이 5개면 5줄이다 — 몇 개를 실제로
확인했는지 개수 자체가 이 기능이 얼마나 검증됐는지를 보여준다. 확인 안 한 규칙은 숨기지 말고
"안 함"이라고 솔직하게 적는다 ("확인 안 한 규칙이 있다"는 사실 자체가 팀장에게 필요한 정보다).

| spec 규칙 번호 | 어떻게 확인했나 (run.tsx의 테스트 이름, 또는 직접 확인한 방법) | 결과 |
|---|---|---|
| 1 |  | 통과 / 실패 / 안 함 |

## 아직 안 되는 것 (Known Issues)

- 없으면 "없음"

## 팀장에게 물어봐야 하는 것

- 없으면 "없음"
