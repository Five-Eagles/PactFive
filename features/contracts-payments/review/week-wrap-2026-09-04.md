# 금주 산출물 정리 — 2026-09-04

담당: 조준영 (`contracts-payments`).
브랜치: `feature/contracts-payments` → PR 대상 `develop`.
기간: 2026-08-31 ~ 2026-09-04.

`app/`은 팀장. A-07 `POST /cancel`·PG 환불·위젯 실연동은 하지 않았다.

## 완료

지난주 백로그(규칙 22: 공개 API Mock, `signContract`, high-fi, invalidate,
`retrievePayment`)를 닫고, 화면 하이브리드와 실행 Mock까지 올렸다.

- 공개 API Mock: 합의·재제안·서명·결제 준비/승인·납품 4종·정산 GET·취소 GET
- 화면: AGR-01·02·03, CTR-01·02, PAY-01·02, DLV-01, SET-01, CAN-01
  (페이지 본문·ViewModel, 앱 셸 없음)
- 규칙 23 납품: `ensureDelivery`, 멱등 키, 승인∧`RELEASED`에서 complete
- 규칙 24 정산 실행: 수수료 스냅샷, ELIGIBLE 1건, `RELEASED` 원자, C-03 409 재판정
- 규칙 25 무효화: 멱등, `paymentPendingAt` 409, 서명 감사 보존, GET `notification`
- 검증: `npx tsx features/contracts-payments/prototype/run.tsx` → **PASS 323 / FAIL 0**

설계서 신설 코드(`AGREEMENT_*`·`CONTRACT_*`·`PAYMENT_*`·`DELIVERY_*`·`SETTLEMENT_*`·
`CANCEL_*`)는 쓰지 않는다. 오류는 규칙 8 5종(+공개 401·403)과 PRD
`PROJECT_CANCEL_AFTER_PAYMENT`.

## 미완료

| 항목 | 상태 |
|---|---|
| Toss sandbox 키 | **미완료.** [teamlead-pg-sandbox-keys.md](teamlead-pg-sandbox-keys.md). Mock만 |
| `app/` 통합 | 팀장. [teamlead-public-api-panels-2026-09-03.md](teamlead-public-api-panels-2026-09-03.md) |
| A-07 `POST /cancel` | 유동우. 이 폴더는 화면 CTA만, 서버 POST 없음 |
| 알림 발송 | 팀장. 조준영은 `publish*`만 |
| 지원 일괄 거절 | 최윤석. GET `applicationRejection`은 `NOT_NEEDED` |
| PG 환불·72시간 동의 | Increment 밖 |

## 위험

- sandbox 키 없으면 다음주도 위젯·실호출 없이 Mock만 검증한다.
- A-07이 오기 전 프로젝트 `CANCELED` 전이는 테스트 헬퍼 `simulateProjectCanceled`다.
- 정산 `simulateSettlementReleased`는 I-30 순서 헬퍼이며 실행 원장 가드를 건너뛴다.

## 다음 주

팀장 `app/` 반영 대기. 키 수신 시에만 위젯 실연동. 유동우 A-07 연동 시 GET cancellation
후처리와 맞출 것. 제외 유지: PG 환불, `INVALIDATED`/`TERMINATED`, 지급 버튼·운영 화면.

## 검증

```bash
npx tsx features/contracts-payments/prototype/run.tsx
```

PASS 323.

## PR 한 줄

합의부터 취소까지 화면 ViewModel과 납품·정산·무효화 실행 Mock을 규칙 23~25로 닫는다. `app/`·A-07·실토스·환불은 다음.
