# 금주 산출물 정리 — 2026-08-28

담당: 조준영 (`contracts-payments` · `reviews`).
브랜치: `feature/contracts-payments` → PR 대상 `develop`.
기간: 2026-08-25 ~ 2026-08-28.

`app/`은 팀장. 리뷰 Mock·위젯 실연동은 하지 않았다.

## 완료

### contracts-payments (규칙 1~9 FACT)

- 4함수 호출 계약: [spec.md](../spec.md) 규칙 1~8, [api-contract.md](../api-contract.md)
- Mock + 검증: [prototype/](../prototype/), [test-report.md](../test-report.md)
  `npx tsx features/contracts-payments/prototype/run.tsx` → **PASS 28 / FAIL 0**
- 교차 회신: [yudong-function-defs-reply.md](yudong-function-defs-reply.md),
  [yoonseok-function-defs-response-final.html](yoonseok-function-defs-response-final.html)

### contracts-payments (규칙 10~22 설계)

합의·서명·샌드박스 결제. 구현은 다음 스프린트.
[spec.md](../spec.md) 규칙 10~22, 공개 API 초안은 [api-contract.md](../api-contract.md).
정본 고정: [spec-design-eval.md](spec-design-eval.md).

잠긴 값: enum은 ERD v1.4. `negotiationId` = `agreements.id`. I-17 결제 1행·FAILED는
같은 행 재시도. 수수료 D-14. 프론트 `/agreements` 5종 폐기.

### reviews (규칙 1~13 설계)

- [features/reviews/spec.md](../../reviews/spec.md)
- [features/reviews/api-contract.md](../../reviews/api-contract.md)
- [features/reviews/index.md](../../reviews/index.md)
- Mock·`design/` 없음 ([test-report.md](../../reviews/test-report.md))

작성은 `COMPLETED`만 (I-24). 방향당 1건·PATCH 없음 (I-23). 태그 10종 E-19.
평균은 공개분 + `REVIEW_CREATED` (E-13). 단독 공개 14일은 ASSUMPTION.

## 미완료

| 항목 | 상태 |
|---|---|
| Toss sandbox 키 | **미완료.** 요청만 [teamlead-pg-sandbox-keys.md](teamlead-pg-sandbox-keys.md). Mock 승인은 확인됨(`pay_mock_ok`). 실호출은 키 없어 해당 없음 |
| `contracts-payments/design/` | 없음 |
| reviews Mock·`design/` | 없음 |
| 알림 4종 (최윤석) | `PAYMENT_COMPLETED` · `DELIVERY_REQUESTED` · `DELIVERY_APPROVED` · `REVIEW_REQUESTED` 미연동 |
| 단독 공개 14일 | ASSUMPTION. 팀장 확정 필요 |

## 위험

- sandbox 키 지연이면 다음 스프린트도 Mock 승인만 검증 가능. 위젯 실연동은 키 수신 후.
- 14일을 바꾸면 reviews 규칙 6·스케줄만 고친다. UNIQUE·COMPLETED 조건은 불변.
- `REVIEW_CREATED` 시점을 INSERT가 아니라 **공개 시점**으로 두었다. 오민혁과 이벤트 계약을
  다음 스프린트에 맞춰야 평균이 새지 않는다.

## 다음 스프린트 백로그

contracts-payments 규칙 22: 공개 API Mock, `signContract` 테스트 2, `design/` low-fi 3화면,
invalidate inbound, `PaymentGateway.retrievePayment`. 제외: 위젯 실연동, 에스크로, PG 환불,
재제안.

reviews 규칙 13: 공개 API Mock, `design/` low-fi 1화면, 14일 스케줄, `REVIEW_CREATED` 발행.
제외: 수정·삭제, 납품 UI, 알림 발송.

## 검증

```bash
npx tsx features/contracts-payments/prototype/run.tsx
```

PASS 28. reviews는 설계만이라 `prototype/run.tsx`가 없다.

## PR 한 줄

합의·서명·결제 SPEC(규칙 10~22)과 리뷰 SPEC(규칙 1~13)을 설계로 닫는다. 구현은 다음 스프린트.
