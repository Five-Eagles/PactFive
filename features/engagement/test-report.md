# engagement 테스트 결과

담당자: 유동우
테스트 날짜: 2026-08-28
테스트한 커밋: `453cb24` 다음 (화면 커밋 포함)

## 자동 검증

- [x] `npx tsx prototype/run.tsx` 통과 (PASS 88 · FAIL 0)
- [x] `npx tsc --noEmit --strict` 통과

```text
1. 저장소 Mock             13건
2. 북마크 추가             11건
3. 북마크 제거              7건
4. 내 북마크 목록          19건
5. 추천 프로젝트           19건
6. 화면 필수 요소 9개      11건
7. 화면 규칙                8건
──────────────────────────────
                           88건
```

## spec.md 규칙별 확인

번호 규칙 34개다. **32개 확인 · 2개 안 함.** 안 한 것은 이유를 아래에 적었다.

### 북마크 토글 (1~8)

| 규칙 | 어떻게 확인했나 | 결과 |
|---|---|---|
| 1 ("1건 있는 상태로 만든다") | 재추가 → `200` · `changed: false` · 행은 여전히 1건 | 통과 |
| 2 ("0건인 상태로 만든다") | 없는 것 제거 → `200` · `changed: false` | 통과 |
| 3 (재추가해도 `created_at` 유지) | 두 응답의 `bookmarkedAt` 이 같음 | 통과 |
| 4 (실제로 지운다) | `remove` 후 `find` 가 `null` | 통과 |
| 5 (프리랜서만) | 비로그인 `401` · 의뢰인 `403` | 통과 |
| 6 (없거나 삭제된 프로젝트는 `404`) | `prj_없음` · `prj_deleted` 각각 | 통과 |
| 7 (마감·취소된 것도 저장 가능) | `prj_closed` 저장 성공 | 통과 |
| 8 (마감돼도 자동으로 안 지운다) | 목록에 `prj_closed` 가 남아 있음 | 통과 |

### 내 북마크 목록 (9~15)

| 규칙 | 어떻게 확인했나 | 결과 |
|---|---|---|
| 9 (본인 것만) | 다른 프리랜서 목록이 1건으로 분리됨 | 통과 |
| 10 (최근 저장순) | `bookmarkedAt` 이 내림차순인지 배열을 훑어 확인 | 통과 |
| 11 (10개/페이지) | 기본 `pageSize` 가 10 | 통과 |
| 12 (삭제된 프로젝트 제외) | `prj_deleted` 없음 · `totalCount` 도 뺀 값 · **거른 뒤 자르는지** 2페이지로 확인 | 통과 |
| 13 (마감된 것은 남는다) | `prj_closed` 가 목록에 있음 | 통과 |
| 14 (마감은 지원만 비활성) | `canApply: false` · 화면에서 버튼 `disabled` | 통과 |
| 15 (빈 목록도 오류 아님) | 시드 0건에서 `200` · `items: []` | 통과 |

### 추천 프로젝트 (16~26)

| 규칙 | 어떻게 확인했나 | 결과 |
|---|---|---|
| 16 (로그인 불필요) | `auth` 없이 호출해 `200` | 통과 |
| 17 (저장하지 않는다) | `recommendations` 저장소가 코드에 없다 | 통과 |
| 18 (후보 3조건) | 자기 자신·삭제·`OPEN` 아닌 것이 결과에 없음 | 통과 |
| 19 (마감은 추천 안 함) | 기술이 겹치는 `prj_reco_closed` 가 빠짐 | 통과 |
| 20 (3단계 우선순위) | 1순위 2건 → 2순위 → 3순위 **순서까지** 확인 | 통과 |
| 21 (동점은 최신순) | 1순위 둘 중 최근 등록된 것이 먼저 | 통과 |
| 22 (4건 고정) | 후보 5건에서 4건만 나옴 · 건수 파라미터 없음 | 통과 |
| 23 (적으면 있는 만큼) | 후보가 적은 프로젝트에서 4건 미만 반환 | 통과 |
| 24 (0건도 오류 아님) | `200` · 화면은 섹션 자체를 렌더링 안 함 | 통과 |
| 25 (없는 프로젝트는 `404`) | `prj_없음` · `prj_deleted` 각각 | 통과 |
| 26 (랭킹·ML 안 함) | 정렬이 3단계 tier + 등록 시각뿐이다 | 통과 |

### 응답에 없어야 하는 것 (27~29)

| 규칙 | 어떻게 확인했나 | 결과 |
|---|---|---|
| 27 (`transactionStatus` 키 없음) | 목록·추천 응답을 `"transactionStatus" in ...` 로 확인 | 통과 |
| 28 (점수·순위값 없음) | 응답과 렌더링된 HTML 양쪽에서 확인 | 통과 |
| 29 (`deletedAt` 없음) | 포트가 애초에 주지 않는다 — 받을 수 없는 값이다 | 통과 |

### 화면 (30~31)

| 규칙 | 어떻게 확인했나 | 결과 |
|---|---|---|
| 30 (보는 사람별 아이콘) | 의뢰인 렌더링 결과가 빈 문자열 · 비로그인은 버튼 있음 · 프리랜서는 `aria-pressed` 반영 | 통과 |
| 31 (실패 시 되돌리기) | `BookmarkButton` 이 `catch` 에서 이전 상태로 되돌리고 `role="alert"` 로 이유를 알린다. **자동 검사는 없다** | 부분 |

### 불변식 (32~34)

| 규칙 | 어떻게 확인했나 | 결과 |
|---|---|---|
| 32 (`(freelancer_id, project_id)` 유일) | Mock 이 UNIQUE 위반을 던지고, 서비스가 잡아서 성공으로 바꾸는 경로를 확인 | 통과 |
| 33 (프리랜서만) | 역할 판정을 포트에 위임 · 의뢰인 `403` | 통과 |
| 34 (프로젝트에 쓰지 않음) | `ProjectReadPort` 에 쓰기 함수가 없다. **타입상 불가능하다** | 통과 |

### 안 한 것

| 규칙 | 왜 |
|---|---|
| 31 (토글 실패 되돌리기) | 코드에는 있으나 **자동 검사가 없다.** 렌더링만으로는 비동기 실패 후 상태 복원을 확인할 수 없다. 클릭 이벤트를 흉내 내는 테스트 도구가 필요하다 |
| 11 (10개/페이지) 중 화면 쪽 | 페이지네이션 UI 를 만들지 않았다. 서버 응답의 `page`·`totalPages` 는 확인했다 |

## 화면 필수 요소

`design/high-fi-bookmarks.html` 의 "필수 요소 목록" **9개 전부** 기본 렌더링에서 나타난다.

| 출처 | 개수 |
|---|---|
| PRD §14 문구표 | 8 |
| PRD §7.2 화면 이름 (`추천 프로젝트`) | 1 |

**출처가 섞여서 시안 표에 근거 열을 뒀다.** project-management 는 전부 §14 정본이라 그 열이 없었다.

추가로 확인하는 것.

- 화면에 원시 색상값(`#RRGGBB`)이 박혀 있지 않다
- 추천 후보가 0건이면 **빈 문자열을 렌더링한다** — 빈 상태 안내조차 남기지 않는다
- 의뢰인에게 북마크 아이콘이 렌더링되지 않는다

## 아직 안 되는 것 (Known Issues)

- **`prototype/server/` 는 구현 초안이다.** 실제 DB 없이 `mock/` 위에서 돈다.
- **`ProjectReadPort` 3종이 project-management 에 아직 없다** (CR-0001).
  Mock 어댑터가 대신하고 있고, 실제 구현이 올라오면 어댑터 한 곳만 교체한다.
- **`web/ui.tsx` 가 project-management 것과 거의 같다.** 기능 폴더 간 직접 import 가
  금지돼 있어(2026-08-28 팀 표준) 두 벌을 따로 뒀다. **통합 단계에서 `app/web/src/shared/`
  로 뺄 후보다** — 지금 한쪽만 고쳐지는 것보다 중복이 안전하다.
- **페이지네이션 UI 가 없다.** 서버는 페이지를 나눠 주지만 화면에 페이지 버튼이 없다.
- 상태별 시각 표현(hover·focus·pressed 등)은 CSS 클래스만 두고 실제 스타일은 넣지 않았다.

## 팀에게 물어봐야 하는 것

| | 대상 | 내용 |
|---|---|---|
| CR-0001 | 유동우 (project-management) | `getProjectCardData` · `getProjectCardDataBulk` · `findRecommendationCandidates` 3종이 필요하다 |

**같은 사람이 만들 것이지만 문서로 남겼다.** 나중에 도메인이 갈라질 때 무엇이 경계를 넘는
호출이었는지 알 수 있어야 한다 (PRD §4.0).
